# 安全策略

## 不要提交的内容

以下内容不得出现在 Issue、Pull Request、提交、日志或测试样本中：

- VISION_API_KEY、DIFY_API_KEY、API_AUTH_TOKEN 等凭据；
- 真实生产图片、个人信息、合同或未脱敏业务数据；
- 可用于访问内部网络或第三方服务的令牌、Cookie 和配置；
- 包含敏感请求头、完整上游响应或密钥片段的日志。

本地配置应从 .env.example 复制到 .env，并确认 .env 不被提交。

## 报告漏洞

如果发现可能导致凭据泄露、未授权访问、敏感数据外发或其他安全影响的问题：

1. 不要创建公开 Issue 或公开 Pull Request；
2. 如果仓库启用了 GitHub Private vulnerability reporting，请通过该私密入口提交；
3. 否则联系仓库维护者的私密渠道，并只提供必要的复现信息。

报告内容建议包括受影响的提交或版本、影响范围、最小复现步骤、临时缓解措施，以及已经脱敏的日志。不要在报告中附上真实密钥或完整敏感样本。

## 运行时建议

生产部署时应配置 API 认证令牌，限制 CORS_ORIGIN，控制视觉模型和 Dify 的网络出口，并建立凭据轮换和访问审计。API_AUTH_TOKEN 为空时不提供应用层认证，不应直接暴露到不受信任的网络。